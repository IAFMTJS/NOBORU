export type CampfireIntensity = "idle" | "enhanced" | "festival" | "advanced";

/** Stub — visuals stripped. */
export function CampCampfire(_props?: { intensity?: CampfireIntensity; className?: string }) {
  return null;
}
