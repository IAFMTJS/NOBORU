/**
 * Re-exports for Supabase database types.
 * Regenerate the full schema file after migrations: npm run db:types
 */
export type {
  Database,
  Json,
  ReviewItemDbRow,
  SubmitReviewRatingRpcResult,
} from "@/lib/supabase/database.types";
