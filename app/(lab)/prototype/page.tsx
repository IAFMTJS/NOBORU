import { PrototypeScreen } from "@/features/prototype/components/prototype-screen";

export const metadata = {
  title: "UI Lab · Noboru",
  description: "Visual UI prototype with mock data — not connected to live services.",
};

export default function PrototypePage() {
  return <PrototypeScreen />;
}
