# UI Lab (prototype)

`app/(lab)/prototype/page.tsx` is a **non-production** visual sandbox.

- Uses mock data via `features/prototype/components/` — not wired to live services.
- Patterns promoted to production belong in `components/visual/` or feature modules.
- Do not treat prototype screens as reference for learner UX or content contracts.
