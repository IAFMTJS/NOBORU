import { LoadingShell } from "@/components/visual/loading";

/** Illustrated full-screen loading splash — camp trail at night (Doc 08). */
export default function AppLoading() {
  return <LoadingShell mode="fullscreen" />;
}
