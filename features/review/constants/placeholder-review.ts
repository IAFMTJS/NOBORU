import type { ReviewCardViewModel } from "@/features/review/types/review.types";

/**
 * Phase 0 placeholder review card.
 * Replace via review.repository.ts when SRS backend is connected.
 */
export const PLACEHOLDER_REVIEW_CARD: ReviewCardViewModel = {
  id: "vocab-001",
  term: "学生",
  reading: "がくせい",
  meaning: "student",
};
