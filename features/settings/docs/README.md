# Settings Feature

## Purpose

User preferences and account configuration.

## Responsibilities

- Settings screen UI with theme selection and sign out
- Server reads via `settings-server.service.ts`
- Client updates via `settings.service.ts` and `settings.repository.ts`

## Known Limitations

- Daily goal and notification toggles are read-only until Phase 3+
